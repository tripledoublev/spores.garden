# Lexicon publish script credentials (for scripts/publish-lexicons-to-repo.js)
# ATPROTO_IDENTIFIER=your-handle-or-email
# ATPROTO_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx
# Optional overrides:
# ATPROTO_AUTH_SERVICE=https://bsky.social
# ATPROTO_PDS_URL=https://your-pds.example
# LEXICON_REPO_DID=did:plc:xxxxxxxxxxxxxxxxxxxx

# Target repo DID (defaults to authenticated DID; used by garden-data-tools.js)
# ATPROTO_REPO_DID=did:plc:xxxxxxxxxxxxxxxxxxxx

# Garden data tools (backup/reset/restore) use the same auth env vars:
# npm run garden:backup
# npm run garden:reset -- --dry-run
# npm run garden:reset -- --yes
# npm run garden:restore -- --from backups/<file>.json
